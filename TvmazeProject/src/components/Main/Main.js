import React from "react";
import { ScrollView } from "react-native";
import ShowsList from "../ShowsList/ShowsList";

const Main = () => {
  return (
    <ScrollView>
      <ShowsList />
    </ScrollView>
  );
};

export default Main;
